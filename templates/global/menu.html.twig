<div class="bg-teal-900 p-5 flex gap-10 align-items-center">
    <h1 class="text-x1 font-bold mb-4 text-white">
        {{ ux_icon("tabler:home") }}
    </h1>
    <ul class="list-unyled flex gap-4 text-white">
        <li class="flex items-center gap-2">
            <a href="{{ path('accueil') }}">home{{ ux_icon("tabler:home") }}</a>
        </li>
        <li class="flex items-center gap-2">
            <a href="{{ path('apropos') }}">{{
                ux_icon("tabler:alert-square-rounded-filled")
            }}</a>
        </li>
        <li class="flex items-center gap-2">
            <a href="{{ path('contact') }}">Contact</a>
        </li>
        <li class="flex items-center gap-2">
            <a href="{{ path('app_soiree_index') }}">soirées</a>
        </li>
        <li class="flex items-center gap-2">
            <a href="{{ path('app_soiree_new') }}">nouvelles soirées</a>
        </li>
        <li class="flex items-center gap-2">
            <a href="{{ path('app_artist_index') }}">artistes</a>
        </li>
        <li class="flex items-center gap-2">
            <a href="{{ path('app_artist_new') }}">nouvel artiste</a>
        </li>
    </ul>
</div>
