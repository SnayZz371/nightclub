{% extends 'base.html.twig' %}

{% block title %}Hello GlobalController!{% endblock %}

{% block body %}
    <div class="container mx-auto px-4 py-8 text-center">
        <h1 class="text-3xl font-bold text-gray-900 dark:text-white mb-4">Welcome {{ title | upper }}! ✅</h1>

        <p class="text-gray-700 dark:text-gray-300 mb-4">This friendly message is coming from:</p>
        <img src="{{ asset('image/soiree.avif') }}" alt="" class="mx-auto">
    </div>
{% endblock %}
