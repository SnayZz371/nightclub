{% extends 'base.html.twig' %}

{% block title %}New Materiel{% endblock %}

{% block body %}
    <div class="container mx-auto px-4 py-8">
        <h1 class="text-3xl font-bold text-gray-900 dark:text-white mb-6">Créer un nouveau matériel</h1>

        <div class="bg-white dark:bg-gray-800 shadow-md rounded-lg p-6">
            {{ include('materiel/_form.html.twig') }}
        </div>

        <div class="mt-6">
            <a href="{{ path('app_materiel_index') }}" class="bg-gray-500 hover:bg-gray-600 text-white font-bold py-2 px-4 rounded transition-colors duration-200">Retour à la liste</a>
        </div>
    </div>
{% endblock %}
